// functions:with args and with  no.return value
#include<stdio.h>
float add();
float add()
{
	float i,n,sum;
	for(i=1;i<n;i++)
	{
		if(i==0)
		sum=sum+i;
	}
	if (sum==n)
	printf("no is perfect");
	else
	   printf("no is not perfect");
}
void main()
{
	int n;
	printf("enter any no");
	scanf("%d",&n);
	
}
